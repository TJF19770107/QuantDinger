MEM｜2026-04-30｜三方前台互通规则摘要（不含敏感值）

- 目标：豆包入站 → Bridge → Trae 前台可见并回复 → Hermes 可见链路事件 → 豆包收到回复
- 前台窗口：Trae 群聊窗口固定 `127.0.0.1:18092`，Bridge 监控 `127.0.0.1:18091/monitor`，Hermes WebUI `127.0.0.1:6060`
- 安全：密钥仅环境变量注入；日志只允许 SET/EMPTY；禁止明文回显 token/secret
- 旁路：`X-Lobster-Local-Test: 1` 仅用于本机验收，仍必须通过 webhook secret 校验
- FRP 验收：必须先具备真实 `FRP_SERVER_ADDR/PORT/TOKEN/REMOTE_PORT`；占位符视为未配置，禁止启动

