【Trae提炼】2026-04-30 | 原文件完整路径：E:\龙虾AI主控中心\.trae\skills\lobster_tripartite_handshake_v1.0\handshake.ps1

# 三方握手闭环测试报告（脱敏）

## 1) 测试目标

- 发起一次三方握手（物理层 TCP → 通信层 HTTP → 应用层版本/索引/令牌 SET/EMPTY）
- 输出可审计报告与链路固化产物（不写入任何密钥明文）

## 2) 测试执行（无副作用模式）

- 执行参数：`-Repair 0`（禁止任何“自动修复/自动写入环境变量/生成密钥”行为）
- 状态目录：显式落盘到允许目录 `E:\Temp\...`（避免写入 `.trae` 被执行环境拦截）

## 3) 结果摘要

### A) HermesPort=18080（ai-hermes-2-1 口径）

- Hermes：TCP TIMEOUT（离线/不可达）
- Doubao：TCP OK，HTTP 404（协议可达）
- Trae：LOCAL（本机）

产物：
- E:\Temp\lobster_state\tripartite_handshake\tripartite_handshake_last_report.json

### B) HermesPort=8642（hermes-agent/gateway 口径）

- Hermes：TCP OK，HTTP 200（/health）
- Doubao：TCP OK，HTTP 404（协议可达）
- Trae：LOCAL（本机）

产物：
- E:\Temp\lobster_state\tripartite_handshake_8642\tripartite_handshake_last_report.json

## 4) 应用层校验（技能/索引/令牌）

- INDEX.jsonl：存在（共享技能库可检索）
- 三技能本地存在性：三项均存在
- 共享文档存在性：
  - handshake：存在（v1.0）
  - self_learning：存在（v1.0）
  - claude_mem_gstack_bridge：已补齐共享文档并更新索引（v1.0）
- 令牌：仅记录 SET/EMPTY（不回显任何值）

## 5) 建议修复/优化项

- Hermes 端口口径需要固化为“可参数化 + 报告证据化”：默认 18080，但当实际栈使用 8642 时必须在握手命令中显式传参。  
- 豆包 /health 返回 404 不影响“协议可达”判断，但建议在豆包服务补齐 /health 200 以便统一健康口径。  

