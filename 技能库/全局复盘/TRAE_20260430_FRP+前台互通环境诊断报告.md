【Trae诊断报告】2026-04-30 | 主题：FRP 穿透 + 三方前台互通（环境诊断）

## 1) 服务运行态（已确认）

- Bridge：`ai-bridge-1` 运行中，宿主映射 `127.0.0.1:18091 -> 8091`
- 豆包服务：`ai-doubao2-1` 运行中，宿主映射 `127.0.0.1:18000 -> 8000`
- Hermes：
  - `hermes-agent` 运行中（端口段映射 `8642-8670`)
  - `hermes-webui` 运行中（`127.0.0.1:6060`)
  - `hermes gateway status` 显示 `Gateway is running`
- Trae 前台群聊窗口：本机 `http://127.0.0.1:18092/`（独立进程，内存态，不落盘）

## 2) 关键配置落地（已确认）

- `docker-compose.lobster.yml`：为 `doubao_bridge` 新增了 Trae inbox 转发参数（enable/url/timeout）。
- `bridge_service.py`：新增 Trae inbox 推送与 reply 回填逻辑；新增 `X-Lobster-Local-Test` 本地验收旁路（仍需 webhook secret）。

## 3) FRP（未通过：占位符阻断）

- FRP 二进制存在：`frpc.exe/frps.exe` 已在仓库内。
- `FRP_SERVER_ADDR / FRP_TOKEN` 当前在 Process 环境变量中仍为占位符（含 `<` `>`），导致 `frp_start_doubao_tunnel.ps1` 门禁拒绝启动。
- 结论：FRP 目前无法自动启动穿透；需要你把 `FRP_SERVER_ADDR/PORT/TOKEN/REMOTE_PORT` 设置为真实值后再验收公网回源。

## 4) Docker 拉取（已知限制）

- `docker compose up --build` 在构建 `wechat_gateway` 时因无法拉取 `python:3.11-slim` 失败（Docker Hub OAuth token 连接超时）。
- 已改为仅启动本链路必需服务（`doubao2` + `doubao_bridge`），不影响本次三方前台互通验收。

