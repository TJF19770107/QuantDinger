【Trae验收报告】2026-04-30 | 主题：FRP 穿透 + 三方前台互通（全链路验收）

## A. 三方前台互通（本地）——已通过

### 1) 前台可见入口

- Trae 群聊窗口：`http://127.0.0.1:18092/`
- Bridge 监控：`http://127.0.0.1:18091/monitor`
- Hermes WebUI：`http://127.0.0.1:6060/`

### 2) 闭环测试用例（前台肉眼可见）

- 用例：本机模拟 webhook → Bridge → Trae 群聊窗口收到并自动回复 → Bridge 回填 reply
- 触发方式：`python -u .\scripts\test_frontend_chain.py`
- 判定：脚本输出 `OK: reply_from_trae_inbox=YES` 且 reply 为 `三方通道已连通，能收到豆包消息`

### 3) 证据点（无需泄露 trigger token）

- Bridge：日志事件包含 `bridge.webhook.recv` 与 `bridge.webhook.trae_reply`
- Trae 群聊窗口：出现 `doubao:` 入站与 `Trae:` 自动回复
- Hermes：Bridge 会异步触发 Hermes `/v1/runs`（用于让 Hermes WebUI 可见链路事件）

## B. FRP 穿透（公网回源）——未通过（前置条件缺失）

- 当前阻断点：`FRP_SERVER_ADDR/FRP_TOKEN` 为占位符（含 `<` `>`），脚本门禁拒绝启动 frpc。
- 通过条件：
  - `FRP_SERVER_ADDR/PORT/TOKEN/REMOTE_PORT` 注入真实值后，运行 `scripts/frp_start_doubao_tunnel.ps1`
  - 公网访问 `http://<FRP_SERVER_ADDR>:<FRP_REMOTE_PORT>/bridge/health` 返回 200

## C. 回滚点（不覆盖旧规则）

- 变更均为合并更新：新增脚本/新增 env 配置/新增技能条目；未删除旧文件。
- 若需要回滚：停止群聊窗口进程、删除新增的 `TRAE_INBOX_*` 环境变量段落并 recreate `doubao_bridge`，即可退回“仅 Hermes reply”的旧行为。

